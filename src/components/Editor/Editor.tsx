import { Component } from "solid-js";

// Editor component is reserved for Phase 2 (source editing).
// Phase 1 uses MarkdownView for read-only rendering.
export const Editor: Component = () => {
  return null;
};
